import 'package:flutter/material.dart';

class ColorConstanse {
  static Color LIGHTGREY = Color(0xff606268);
  static Color green = Color(0xff0FBB6B);
  static Color white = Colors.white;
  static Color mainblack = Colors.black;
  static Color blue = Colors.blue;
  static Color red = Colors.red;
  static Color grey = Colors.grey;

  //
  static const primaryColor = Color(0xFF007AFF); // Bright blue
  static const iconColor = Color(0xFF333333); // Darker shade for icons
  static const searchFieldBackground =
      Color(0xFFF2F3F5); // Light gray for search field
  static const lightBackground =
      Color(0xFFFAFAFA); // Light background for main screen
  static const primaryTextColor =
      Color(0xFF1E1E1E); // Darker primary text color
  static const labelColor = Color(0xFF757575); // Subtle color for label text
  static const borderColor = Color(0xFFE0E0E0); // Border color for input fields
  static const carouselBackground =
      Color(0xFFF0A500); // Accent color for carousel
  static const tabSelectedColor = Color(0xFF007AFF); // Active tab color
  static const tabUnselectedColor = Color(0xFF9E9E9E); // Inactive tab color
}
